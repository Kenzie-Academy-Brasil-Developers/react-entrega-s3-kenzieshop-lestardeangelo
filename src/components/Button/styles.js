import styled from "styled-components";

export const Container = styled.button`
    color: var(--black);
    height: 21px;
    border-radius: 5px;
    border: 1px solid var(--black);
    width: 8vh;
    font-size: 13px;

`